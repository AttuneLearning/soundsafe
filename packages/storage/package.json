{
  "name": "@soundsafe/storage",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "IndexedDB + OPFS wrappers + quota watchdog. Per-shell secret store.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "echo 'no tests yet (M0)'"
  },
  "dependencies": {
    "@soundsafe/platform": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
