// @soundsafe/ui-kit — presentational React components.
//
// Components land here in M1+ as the consumer-app needs them. ADR-004 seams
// (AssignmentProvider + ProgressSink TS interfaces with v1 no-op impls) live
// in this package, not in consumer-app.

export const __PACKAGE_NAME = '@soundsafe/ui-kit';
