{
  "name": "@soundsafe/roadmap-schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas for Roadmap, Step, Transform, TransformParams, Manifest. Generated from sfx-pack-manifest.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "echo 'no tests yet (M0)'",
    "generate": "echo 'TODO: cargo run -p sfx-pack-manifest --features emit-schema | json-schema-to-zod (lands in M1)'"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
