{
  "name": "@soundsafe/audio-graph-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Web Audio + AudioWorklet + WASM bridge. The only TS package that imports rust-core.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "echo 'no tests yet (M0)'"
  },
  "dependencies": {
    "@soundsafe/platform": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
