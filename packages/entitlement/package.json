{
  "name": "@soundsafe/entitlement",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stripe + RS256-JWT entitlement client. JWKS verification, refresh-token flow.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "echo 'no tests yet (M0)'"
  },
  "dependencies": {
    "@soundsafe/platform": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
