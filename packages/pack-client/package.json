{
  "name": "@soundsafe/pack-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CDN fetch + Service Worker pack cache + decrypt-worker orchestration.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --config eslint.config.js --max-warnings 0"
  },
  "dependencies": {
    "@soundsafe/platform": "workspace:*",
    "@soundsafe/roadmap-schema": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "eslint": "^9.13.0",
    "msw": "^2.4.9",
    "typescript": "^5.5.4",
    "vitest": "^2.1.4"
  }
}
