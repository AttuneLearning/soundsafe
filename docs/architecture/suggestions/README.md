# Suggestions

Proposed architecture changes awaiting review. File naming: `YYYY-MM-DD_{area}_{slug}.md`.

A suggestion that is accepted graduates to an ADR in `../decisions/` and the suggestion file is archived (either deleted or marked `status: accepted-and-archived`).

This directory is empty at bootstrap.
