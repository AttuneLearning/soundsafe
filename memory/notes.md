# Quick Notes

Low-ceremony captures. Appended by `/memory note <text>`. Promote to entities / patterns / context when they warrant it.

- **2026-04-19**: Soundsafe repo bootstrapped with architecture docs, feature matrix, and 17 seed ADRs (D1–D17). No code yet. Next step: add `ai_team_config` submodule and wire `/memory` + `/adr` skills.
